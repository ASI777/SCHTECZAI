export const MOCK_DELAY = 800;

export const INITIAL_LOGS = [
  { timestamp: Date.now(), message: "System initialized. Waiting for user input...", type: 'info', system: 'CLIENT' }
];

export const PLACEHOLDER_FOOTPRINT = "https://picsum.photos/400/400"; // Fallback